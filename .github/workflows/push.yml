name: Push Trigger
run-name: 🚀 Push.yml
on:
  push:
    branches:
      - main

jobs:
  show-info:
    runs-on: ubuntu-latest
    steps:
      - name: Affichage des infos système
        run: |
          echo "Déclenché par PUSH"
          uname -a
          whoami
          env | sort