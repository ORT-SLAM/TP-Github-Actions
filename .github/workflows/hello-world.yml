name: Hello Github Actions

on:
  push: # Lorsqu'on push un commit
    branches: [main]
  pull_request: # Lorsqu'on crée une pull request
    branches: [main]
  # schedule: Lorsqu'on dispatch un workflow
    # - cron: "0 10 * * 1" # tous les lundis à 10h
  workflow_dispatch:

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Affichage d'un message
        run: echo "Hello Github Actions depuis le workflow !"
